{
  "name": "mapakit",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
